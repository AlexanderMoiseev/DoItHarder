%h1 Listing exercises
= link_to 'New exercise', new_exercise_path

%table
  %tr
    %th Title 
    %th Text 

  - @exercises.each do |exercise|
    %tr
      %td= exercise.name
      %td= link_to 'Start Exercise', exercises_start_exercise_path(exercise)
      %td= link_to 'Show', exercise_path(exercise)
      %td= link_to 'Edit', edit_exercise_path(exercise)
      %td= link_to 'Destroy', exercise_path(exercise), 
                              method: :delete, 
                              data: { confirm: 'Are you sure?' }